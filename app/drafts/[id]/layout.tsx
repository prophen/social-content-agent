import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Edit draft | Social Content Agent",
};

export default function DraftEditorLayout({ children }: { children: React.ReactNode }) {
  return children;
}
