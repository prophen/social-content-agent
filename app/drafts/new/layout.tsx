import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "New draft | Social Content Agent",
};

export default function NewDraftLayout({ children }: { children: React.ReactNode }) {
  return children;
}
